# Phase 0.5: Fstab Recovery Plan (scaffold)
# Citation: [archwiki:Fstab]

name: fstab
description: Validate and repair /etc/fstab
citation: "[archwiki:Fstab]"

steps:
  - name: backup_fstab
    description: Create backup of current fstab

  - name: validate_syntax
    description: Check fstab syntax

  - name: regenerate_uuids
    description: Update UUIDs if needed
    destructive: true
