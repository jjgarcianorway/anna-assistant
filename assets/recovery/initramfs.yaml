# Phase 0.5: Initramfs Recovery Plan (scaffold)
# Citation: [archwiki:Mkinitcpio]

name: initramfs
description: Rebuild initramfs images
citation: "[archwiki:Mkinitcpio]"

steps:
  - name: backup_current
    description: Backup current initramfs

  - name: rebuild_initramfs
    description: Rebuild with mkinitcpio -P
    destructive: true

  - name: verify_images
    description: Verify generated images exist
