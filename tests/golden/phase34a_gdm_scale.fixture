# Phase 34A: Golden fixture for "can you please scale up GDM login screen?"
# Expected: Either ConfirmationRequired (Mutating) OR Abstained with prerequisite
# MUST NOT contain: "could not format", evaluator text (EXPLANATION:, ANNA: NONE)
# MUST NOT repeat: "Config changed: group" banner per turn

# Question: can you please scale up GDM login screen?
# Routes to: display.scale.gdm (Mutating capability)

# Scenario A: Prerequisites met (GDM active, user monitors.xml exists with scale)
{"Step":{"step":{"step_type":"ConfirmationRequest","content":"Detected:\n  This will copy your monitors.xml to GDM so the login screen uses the same scaling (2) as your session.\n\nPlan:\n  Step 1: Create GDM configuration directory [requires approval]\n  Step 2: Copy display configuration to GDM [requires approval]\n  Step 3: Set file ownership for GDM [requires approval]\n\nAnna: Proceed? (yes/no)"}}}
{"Done":{"result":{"answer":"Detected:\n  This will copy your monitors.xml to GDM so the login screen uses the same scaling (2) as your session.\n\nPlan:\n  Step 1: Create GDM configuration directory [requires approval]\n  Step 2: Copy display configuration to GDM [requires approval]\n  Step 3: Set file ownership for GDM [requires approval]\n\nAnna: Proceed? (yes/no)","success":true,"iterations":0,"commands_executed":[],"dialogue":[],"needs_clarification":true,"clarification_question":"Proceed? (yes/no)","cached":false,"citations":[],"abstained":false,"final_confidence":null}}}

# Scenario B: Prerequisites NOT met (wrong DM or missing config)
# {"Step":{"step":{"step_type":"FinalAnswer","content":"Your system uses SDDM. GDM scaling is not applicable.\n\nTo configure SDDM scaling, the ServerArguments DPI setting in /etc/sddm.conf is typically used."}}}
# {"Done":{"result":{"answer":"Your system uses SDDM. GDM scaling is not applicable.\n\nTo configure SDDM scaling, the ServerArguments DPI setting in /etc/sddm.conf is typically used.","success":false,"iterations":0,"commands_executed":[],"dialogue":[],"needs_clarification":false,"clarification_question":null,"cached":false,"citations":[],"abstained":true,"final_confidence":null}}}

# CONTRACT ENFORCEMENT:
# - commands_executed MUST be empty (no LLM invocation)
# - MUST NOT contain: "could not format a valid response"
# - MUST NOT contain: "gathered information but"
# - MUST NOT contain evaluator text: "EXPLANATION:", "ANNA: NONE", "COMPLETE", "INCOMPLETE"
# - Step descriptions only, no shell syntax (no mkdir, cp, chown visible)
# - Single clear outcome: ConfirmationRequired OR Abstained with ONE actionable prerequisite

