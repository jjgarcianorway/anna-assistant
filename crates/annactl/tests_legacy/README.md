# Legacy Tests (Archived)

**Legacy test, not part of 6.0.0. Kept for reference.**

This directory contains tests for features that are not part of the 6.0.0 release:
- TUI streaming tests (Beta.280)
- Other deprecated feature tests

These tests are excluded from the default test run.

For active tests, see `crates/annactl/tests/` and `crates/anna_common/tests/`.
